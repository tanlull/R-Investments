#######################################
#                                     #
#     Exercise Visualization          #
#                                     #
#######################################

# 1. Load 'diamonds' dataset from ggplot2 package. Check the 3 first rows of the dataset

l------(g------)          #load ggplot2 package
d---(d-------)            #load 'diamonds' dataset
h---(--------, n = -)            #check the 3 first rows of the dataset

# 2. Plot a scatter plot of the diamond price vs diamond width ("y" vairable). 
# x = length, y = width, z = depth

plot(x = , y = )


# 3. Create the same plot with customization

plot(x = , y = ,                     #specify what's on x-axis and y-axis
     main = ,                        #chart title
     xlab = ,                        #x-axis label
     ylab = ,                        #y-axis label
     col  = )                        #plot color
